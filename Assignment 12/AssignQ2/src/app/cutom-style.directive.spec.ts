import { CutomStyleDirective } from './cutom-style.directive';

describe('CutomStyleDirective', () => {
  it('should create an instance', () => {
    const directive = new CutomStyleDirective();
    expect(directive).toBeTruthy();
  });
});
